services:
  - name: discord-anti-spam-bot
    type: worker
    runtime: node
    region: oregon
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
      - key: LOG_CHANNEL_ID
        sync: false
